# HSE Encoder Dependencies
# CPU-ONLY - No CUDA

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.0.0

# CPU-only torch (kept for compatibility but not used for HDC)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# NumPy for HDC operations (replacing torchhd)
numpy>=1.24.0

# Async Redis client
redis
aiohttp>=3.9.0
