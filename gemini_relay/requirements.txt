# Gemini Live WebSocket Relay - Dependencies
# Minimal footprint for micro VPS (0.1 vCPU / 512MB RAM)

# Core WebSocket and ASGI
websockets>=12.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Async HTTP client
httpx>=0.26.0

# Optional: Process/memory monitoring (uncomment for production)
# psutil>=5.9.0

# Optional: Azure Key Vault (if not using environment variable)
# azure-identity>=1.15.0
# azure-keyvault-secrets>=4.7.0
